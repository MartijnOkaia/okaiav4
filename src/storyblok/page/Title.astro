---

import { storyblokEditable } from '@storyblok/astro'

const { blok } = Astro.props

---


<div {...storyblokEditable(blok)} class={`title-block py-28 bg-${blok.background}`}>

    <div class="mx-auto px-[30px] max-w-[1600px]">

        <h1 class="animate text-xl max-w-[770px] font-medium inline-block">
            {blok.title}
            <a class="button ml-8 translate-y-3" href={blok.button.cached_url}><span>{blok.buttonText}</span></a>
        </h1>

    </div>

</div> 

<style is:global>
    main > div:first-of-type.title-block{
        padding-top: 10rem !important;
    }
</style>